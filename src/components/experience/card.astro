---
interface Props {
  title: string;
  company: string;
  period: string;
  description: string;
  technologies: string[];
  isLatest?: boolean;
  isLast: boolean;
}

const { title, company, period, description, technologies, isLatest = false, isLast = false } = Astro.props;
---

<div class={`experience-card relative pl-8 sm:pl-10 ${isLast ? "pb-0" : "pb-10"}`}>
  {/* Línea vertical */}
  <div class="absolute left-0 top-8 bottom-0 w-0.5 bg-gray-300 dark:bg-gray-700"></div>

  {/* Punto indicador */}
  <div
    class={`absolute left-[-8px] top-2 w-4 h-4 rounded-full border-2 border-primary ${isLatest ? "bg-blue-400" : "bg-primary"}`}
  >
  </div>

  <div
    class="bg-white dark:bg-gray-800 p-6 rounded-lg shadow-md border border-gray-200 dark:border-gray-700 hover:shadow-lg transition-all duration-300"
  >
    <div class="flex flex-col">
      <span class="text-sm font-medium text-gray-500 dark:text-gray-400">{period}</span>
      <h3 class="text-xl font-bold text-gray-900 dark:text-white mt-1">{title}</h3>
      <h4 class="text-lg font-semibold text-primary mb-2">{company}</h4>

      <p class="text-gray-700 dark:text-gray-300 mb-4">
        {description}
      </p>

      <div class="flex flex-wrap gap-2 mt-auto">
        {
          technologies.map((tech) => (
            <span class="px-3 py-1 text-xs font-medium bg-gray-100 dark:bg-gray-700 text-gray-800 dark:text-gray-200 rounded-full">
              {tech}
            </span>
          ))
        }
      </div>
    </div>
  </div>
</div>
